<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>For You 💗</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      min-height: 100vh;
      font-family: "Poppins", "Segoe UI", Arial, sans-serif;
      overflow-x: hidden;
      background: linear-gradient(135deg, #ffd1dc, #ffe6f2, #d8b4ff, #ffc8dd);
      background-size: 400% 400%;
      animation: gradientMove 10s ease infinite;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 24px;
      color: #4a1833;
    }

    @keyframes gradientMove {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    .page {
      width: 100%;
      max-width: 880px;
      min-height: 620px;
      position: relative;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .card {
      width: 100%;
      max-width: 620px;
      padding: 42px 34px;
      border-radius: 34px;
      background: rgba(255, 255, 255, 0.38);
      backdrop-filter: blur(18px);
      -webkit-backdrop-filter: blur(18px);
      border: 1px solid rgba(255, 255, 255, 0.55);
      box-shadow:
        0 24px 70px rgba(173, 70, 130, 0.28),
        inset 0 0 30px rgba(255, 255, 255, 0.35);
      text-align: center;
      position: relative;
      z-index: 5;
      animation: cardFloat 4s ease-in-out infinite;
    }

    @keyframes cardFloat {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }

    .mini-text {
      display: inline-block;
      padding: 9px 16px;
      background: rgba(255, 255, 255, 0.5);
      border-radius: 999px;
      font-size: 14px;
      font-weight: 600;
      letter-spacing: 0.5px;
      color: #a32060;
      margin-bottom: 22px;
      box-shadow: 0 8px 18px rgba(255, 105, 180, 0.18);
    }

    h1 {
      font-size: clamp(38px, 8vw, 76px);
      line-height: 1;
      margin-bottom: 20px;
      color: #d63384;
      text-shadow: 0 5px 20px rgba(214, 51, 132, 0.25);
    }

    .heart-main {
      width: 92px;
      height: 92px;
      margin: 0 auto 24px;
      position: relative;
      transform: rotate(-45deg);
      background: #ff4d8d;
      animation: heartbeat 1.2s ease-in-out infinite;
      box-shadow: 0 0 35px rgba(255, 77, 141, 0.65);
    }

    .heart-main::before,
    .heart-main::after {
      content: "";
      width: 92px;
      height: 92px;
      position: absolute;
      border-radius: 50%;
      background: #ff4d8d;
    }

    .heart-main::before {
      top: -46px;
      left: 0;
    }

    .heart-main::after {
      left: 46px;
      top: 0;
    }

    @keyframes heartbeat {
      0%, 100% { transform: rotate(-45deg) scale(1); }
      25% { transform: rotate(-45deg) scale(1.08); }
      45% { transform: rotate(-45deg) scale(0.96); }
      65% { transform: rotate(-45deg) scale(1.12); }
    }

    .message {
      min-height: 94px;
      font-size: clamp(18px, 3vw, 24px);
      line-height: 1.7;
      font-weight: 500;
      color: #5b1b3a;
      margin-bottom: 28px;
    }

    .cursor {
      display: inline-block;
      width: 3px;
      height: 1em;
      background: #d63384;
      margin-left: 4px;
      vertical-align: -2px;
      animation: blink 0.8s infinite;
    }

    @keyframes blink {
      0%, 50% { opacity: 1; }
      51%, 100% { opacity: 0; }
    }

    .love-button {
      border: none;
      outline: none;
      cursor: pointer;
      padding: 16px 26px;
      border-radius: 999px;
      background: linear-gradient(135deg, #ff4d8d, #ff85a2, #c77dff);
      color: white;
      font-size: 17px;
      font-weight: 700;
      box-shadow: 0 14px 30px rgba(255, 77, 141, 0.38);
      transition: 0.25s ease;
    }

    .love-button:hover {
      transform: translateY(-4px) scale(1.03);
      box-shadow: 0 18px 38px rgba(255, 77, 141, 0.48);
    }

    .love-button:active {
      transform: scale(0.96);
    }

    .footer-text {
      margin-top: 26px;
      font-size: 14px;
      color: rgba(91, 27, 58, 0.72);
    }

    .floating-heart {
      position: fixed;
      bottom: -40px;
      font-size: 22px;
      animation: floatUp linear forwards;
      pointer-events: none;
      z-index: 1;
      filter: drop-shadow(0 8px 12px rgba(255, 77, 141, 0.25));
    }

    @keyframes floatUp {
      0% {
        transform: translateY(0) translateX(0) rotate(0deg);
        opacity: 0;
      }
      10% {
        opacity: 1;
      }
      100% {
        transform: translateY(-115vh) translateX(var(--move)) rotate(360deg);
        opacity: 0;
      }
    }

    .sparkle {
      position: fixed;
      width: 8px;
      height: 8px;
      background: white;
      border-radius: 50%;
      pointer-events: none;
      z-index: 10;
      animation: sparklePop 900ms ease-out forwards;
      box-shadow: 0 0 16px white;
    }

    @keyframes sparklePop {
      0% {
        transform: scale(0);
        opacity: 1;
      }
      100% {
        transform: scale(3);
        opacity: 0;
      }
    }

    .blob {
      position: fixed;
      border-radius: 50%;
      filter: blur(10px);
      opacity: 0.38;
      z-index: 0;
      animation: blobMove 8s ease-in-out infinite;
    }

    .blob.one {
      width: 180px;
      height: 180px;
      background: #ff85a2;
      top: 8%;
      left: 8%;
    }

    .blob.two {
      width: 220px;
      height: 220px;
      background: #c77dff;
      bottom: 8%;
      right: 7%;
      animation-delay: 2s;
    }

    .blob.three {
      width: 140px;
      height: 140px;
      background: #fff0f6;
      top: 18%;
      right: 16%;
      animation-delay: 4s;
    }

    @keyframes blobMove {
      0%, 100% { transform: translate(0, 0) scale(1); }
      50% { transform: translate(20px, -25px) scale(1.08); }
    }

    @media (max-width: 600px) {
      body {
        padding: 16px;
        align-items: flex-start;
      }

      .page {
        min-height: 100vh;
        padding-top: 46px;
      }

      .card {
        padding: 34px 22px;
        border-radius: 28px;
      }

      .heart-main {
        width: 74px;
        height: 74px;
      }

      .heart-main::before,
      .heart-main::after {
        width: 74px;
        height: 74px;
      }

      .heart-main::before {
        top: -37px;
      }

      .heart-main::after {
        left: 37px;
      }

      .love-button {
        width: 100%;
        max-width: 310px;
      }
    }
  </style>
</head>

<body>
  <div class="blob one"></div>
  <div class="blob two"></div>
  <div class="blob three"></div>

  <div class="page">
    <div class="card">
      <div class="mini-text">A little page made with love ✨</div>

      <h1>For You</h1>

      <div class="heart-main"></div>

      <p class="message">
        <span id="typeText"></span><span class="cursor"></span>
      </p>

      <button class="love-button" onclick="showLove()">
        Tap for more love 💗
      </button>

      <div class="footer-text">
        You are someone special. Always remember that 🌷
      </div>
    </div>
  </div>

  <script>
    const text =
      "I made this tiny page just for you. You make ordinary days feel softer, warmer, and a little more magical 💕";

    const typeText = document.getElementById("typeText");
    let index = 0;

    function typeWriter() {
      if (index < text.length) {
        typeText.textContent += text.charAt(index);
        index++;
        setTimeout(typeWriter, 45);
      }
    }

    typeWriter();

    const hearts = ["💗", "💖", "💘", "💕", "💞", "💓", "🌷", "✨"];

    function createFloatingHeart() {
      const heart = document.createElement("div");
      heart.className = "floating-heart";
      heart.textContent = hearts[Math.floor(Math.random() * hearts.length)];

      heart.style.left = Math.random() * 100 + "vw";
      heart.style.animationDuration = Math.random() * 4 + 5 + "s";
      heart.style.fontSize = Math.random() * 18 + 18 + "px";
      heart.style.setProperty("--move", Math.random() * 120 - 60 + "px");

      document.body.appendChild(heart);

      setTimeout(() => {
        heart.remove();
      }, 9000);
    }

    setInterval(createFloatingHeart, 420);

    function showLove() {
      const messages = [
        "You are loved 💗",
        "You are precious 🌷",
        "You make life brighter ✨",
        "Sending tiny hearts to you 💕",
        "You deserve soft days 🫶"
      ];

      const randomMessage =
        messages[Math.floor(Math.random() * messages.length)];

      typeText.textContent = randomMessage;

      for (let i = 0; i < 24; i++) {
        setTimeout(() => {
          createSparkle(
            window.innerWidth / 2 + Math.random() * 240 - 120,
            window.innerHeight / 2 + Math.random() * 240 - 120
          );
        }, i * 35);
      }

      for (let i = 0; i < 12; i++) {
        setTimeout(createFloatingHeart, i * 80);
      }
    }

    function createSparkle(x, y) {
      const sparkle = document.createElement("div");
      sparkle.className = "sparkle";
      sparkle.style.left = x + "px";
      sparkle.style.top = y + "px";

      document.body.appendChild(sparkle);

      setTimeout(() => {
        sparkle.remove();
      }, 900);
    }

    document.addEventListener("click", function (event) {
      createSparkle(event.clientX, event.clientY);
    });
  </script>
</body>
</html>
